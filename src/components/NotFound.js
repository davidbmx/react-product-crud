import React from "react";

const NotFound = () => (
  <div className="alert alert-dismissible alert-warning">
    <h4 className="alert-heading">Page Not Found!</h4>
    <p className="mb-0">Page does't exist</p>
  </div>
);

export default NotFound;
